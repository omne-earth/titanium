schema_version = "1.2"

[task]
name = "titanium/agent-timeout-wedge"
description = "Agent runs a command that blocks indefinitely (TCP connect to a dead IP), triggering the agent timeout."
keywords = ["timeout", "wedge", "reproduction"]

[agent]
timeout_sec = 30.0
