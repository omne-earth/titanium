schema_version = "1.2"

[task]
name = "titanium/agent-timeout-sleep"
description = "Agent runs a long-running command that exceeds the agent timeout."
keywords = ["timeout", "reproduction"]

[agent]
timeout_sec = 30.0
