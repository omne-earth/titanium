schema_version = "1.1"
artifacts = ["/app/report.json"]

[task]
name = "titanium/verify-krun-podman-env-airgapped"
description = "In-guest probe of the krun-podman environment when air-gapped: egress denial on the internal network, identity, engine-socket absence, write boundaries, and the microVM signatures (no NIC, hypervisor flag, /dev/vsock) under rootless podman + krun."
keywords = ["smoke", "sandbox", "krun", "podman"]
[[task.authors]]
name = "Shree Raj Shrestha"
email = "shree@omne.earth"

[metadata]
author_name = "Shree Raj Shrestha"
author_email = "shree@omne.earth"
difficulty = "easy"
category = "infrastructure"
tags = ["smoke", "sandbox", "krun", "podman"]
expert_time_estimate_min = 5.0
junior_time_estimate_min = 15.0

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 300.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 1024
storage_mb = 5120
gpus = 0
allow_internet = false
mcp_servers = []

[verifier.env]

[environment.env]

[solution.env]
